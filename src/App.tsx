import { Button } from 'components/Button'
import { Suspense } from 'react'

const App = () => {
  return (
    <Suspense>
      React Application
      <br />
      <Button />
    </Suspense>
  )
}

export default App
